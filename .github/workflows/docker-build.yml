name: Docker Build & Push

on:
  push:
    branches:
      - main
      - master
      - develop
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Build Docker image
        run: |
          cd Front-End
          docker build -t shophub:${{ github.sha }} -t shophub:latest .
      
      - name: Test container
        run: |
          docker run -d --name test-app -e PORT=8080 -p 8080:8080 shophub:latest
          sleep 2
          curl -f http://localhost:8080/api/products || exit 1
          docker logs test-app
          docker stop test-app

  # Uncomment below and add your Docker Hub credentials to deploy automatically
  # push:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v3
  #     - uses: docker/setup-buildx-action@v2
  #     - uses: docker/login-action@v2
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}
  #     - uses: docker/build-push-action@v4
  #       with:
  #         context: ./Front-End
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/shophub:latest
  #           ${{ secrets.DOCKER_USERNAME }}/shophub:${{ github.sha }}
